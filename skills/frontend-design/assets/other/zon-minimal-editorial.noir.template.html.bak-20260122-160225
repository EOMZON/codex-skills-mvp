<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Title · Noir</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      :root {
        --black: #0b0c10;
        --white: #f4f4f3;
        --gray-100: #11131a;
        --gray-200: #1c2030;
        --gray-400: #7f8698;
        --gray-600: #b2b6c2;
        --gray-800: #e5e6ea;
        --accent: #d7c6a6;
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
          "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif;
        background: var(--black);
        color: var(--white);
        line-height: 1.8;
        font-size: 16px;
        -webkit-font-smoothing: antialiased;
        text-rendering: optimizeLegibility;
        color-scheme: dark;
      }

      body::before {
        content: "";
        position: fixed;
        inset: 0;
        pointer-events: none;
        background: radial-gradient(
            1200px 800px at 30% -10%,
            rgba(215, 198, 166, 0.08),
            transparent 60%
          ),
          radial-gradient(
            900px 700px at 90% 10%,
            rgba(127, 134, 152, 0.08),
            transparent 55%
          );
        opacity: 1;
        mix-blend-mode: screen;
      }

      ::selection {
        background: var(--accent);
        color: var(--black);
      }

      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 0 24px;
      }

      /* Header */
      .header {
        position: sticky;
        top: 0;
        z-index: 100;
        background: rgba(11, 12, 16, 0.78);
        backdrop-filter: blur(12px);
        border-bottom: 1px solid var(--gray-200);
      }

      .header-inner {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px 24px;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .logo {
        font-size: 11px;
        letter-spacing: 4px;
        text-transform: uppercase;
        font-weight: 600;
        color: var(--gray-600);
      }

      .tag {
        font-size: 10px;
        letter-spacing: 2px;
        text-transform: uppercase;
        padding: 6px 12px;
        border: 1px solid var(--gray-200);
        color: var(--gray-600);
      }

      /* Hero */
      .hero {
        max-width: 800px;
        margin: 0 auto;
        padding: 100px 24px 80px;
        text-align: center;
      }

      .hero-label {
        font-size: 11px;
        letter-spacing: 6px;
        text-transform: uppercase;
        color: var(--gray-400);
        margin-bottom: 24px;
      }

      .hero h1 {
        font-size: clamp(32px, 6vw, 56px);
        font-weight: 300;
        letter-spacing: 2px;
        line-height: 1.2;
        margin-bottom: 16px;
      }

      .hero h1 strong {
        font-weight: 650;
      }

      .hero-sub {
        font-size: 14px;
        color: var(--gray-600);
        letter-spacing: 1px;
      }

      .hero-sub span {
        margin: 0 8px;
        color: var(--gray-400);
      }

      /* Card */
      .card {
        border: 1px solid var(--gray-200);
        background: rgba(17, 19, 26, 0.6);
        padding: 40px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 40px;
      }

      .card h3 {
        font-size: 10px;
        letter-spacing: 3px;
        text-transform: uppercase;
        color: var(--gray-400);
        margin-bottom: 12px;
        font-weight: 650;
      }

      .card p {
        font-size: 14px;
        color: var(--gray-800);
        line-height: 1.8;
      }

      .traits {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-top: 8px;
      }

      .trait {
        font-size: 12px;
        padding: 6px 14px;
        background: var(--white);
        color: var(--black);
        letter-spacing: 1px;
      }

      /* Conversation */
      .conversation {
        max-width: 800px;
        margin: 0 auto;
        padding: 0 24px 100px;
      }

      .conv-header {
        text-align: center;
        padding-bottom: 60px;
        border-bottom: 1px solid var(--gray-200);
        margin-bottom: 60px;
      }

      .conv-header h2 {
        font-size: 11px;
        letter-spacing: 6px;
        text-transform: uppercase;
        color: var(--gray-400);
        font-weight: 650;
      }

      .dialog {
        margin-bottom: 48px;
      }

      .dialog:last-of-type {
        margin-bottom: 0;
      }

      .speaker {
        font-size: 11px;
        letter-spacing: 3px;
        text-transform: uppercase;
        font-weight: 650;
        margin-bottom: 12px;
        display: flex;
        align-items: center;
        gap: 12px;
        color: var(--gray-600);
      }

      .speaker::after {
        content: "";
        flex: 1;
        height: 1px;
        background: var(--gray-200);
      }

      .speaker.q {
        color: var(--gray-400);
      }

      .speaker.a {
        color: var(--white);
      }

      .content {
        font-size: 17px;
        line-height: 2;
        color: var(--gray-800);
      }

      .content.answer {
        color: var(--white);
        font-size: 18px;
      }

      .content + .content {
        margin-top: 16px;
      }

      .highlight {
        background: linear-gradient(
          transparent 62%,
          rgba(215, 198, 166, 0.16) 62%
        );
        padding: 0 2px;
      }

      blockquote {
        margin: 24px 0;
        padding: 24px 32px;
        border-left: 3px solid var(--accent);
        background: rgba(17, 19, 26, 0.75);
        font-style: italic;
        color: var(--gray-800);
      }

      /* Divider */
      .divider {
        max-width: 800px;
        margin: 0 auto;
        padding: 0 24px;
      }

      .divider-line {
        border: none;
        border-top: 1px solid var(--gray-200);
        margin: 60px 0;
      }

      /* Closing */
      .closing {
        max-width: 800px;
        margin: 0 auto;
        padding: 0 24px 100px;
        text-align: center;
      }

      .closing-quote {
        font-size: clamp(20px, 4vw, 28px);
        font-weight: 300;
        line-height: 1.6;
        color: var(--white);
        max-width: 600px;
        margin: 0 auto 32px;
      }

      .closing-attr {
        font-size: 12px;
        letter-spacing: 2px;
        text-transform: uppercase;
        color: var(--gray-400);
      }

      /* Footer */
      footer {
        border-top: 1px solid var(--gray-200);
        padding: 40px 24px;
        text-align: center;
      }

      .footer-inner {
        max-width: 800px;
        margin: 0 auto;
      }

      .footer-note {
        font-size: 12px;
        color: var(--gray-600);
        line-height: 1.8;
        max-width: 520px;
        margin: 0 auto 24px;
      }

      .footer-meta {
        font-size: 11px;
        letter-spacing: 2px;
        text-transform: uppercase;
        color: var(--gray-400);
      }

      /* Responsive */
      @media (max-width: 640px) {
        .hero {
          padding: 60px 24px 50px;
        }

        .card {
          grid-template-columns: 1fr;
          gap: 32px;
          padding: 32px 24px;
        }

        .content {
          font-size: 16px;
        }

        .content.answer {
          font-size: 17px;
        }
      }

      /* Print */
      @media print {
        .header {
          position: static;
          background: transparent;
          backdrop-filter: none;
        }

        body {
          background: #fff;
          color: #000;
          font-size: 12pt;
        }

        body::before {
          display: none;
        }
      }
    </style>
  </head>
  <body>
    <header class="header">
      <div class="header-inner">
        <div class="logo">Project / Series</div>
        <div class="tag">Noir Edition</div>
      </div>
    </header>

    <section class="hero">
      <p class="hero-label">A Conversation With</p>
      <h1><strong>中文标题</strong><br />English Title</h1>
      <p class="hero-sub">身份 <span>/</span> 身份 <span>/</span> 身份</p>
    </section>

    <section class="container" style="padding-bottom: 80px">
      <div class="card">
        <div>
          <h3>About</h3>
          <p>这里写简短人物介绍、背景、上下文。</p>
        </div>
        <div>
          <h3>Key Traits</h3>
          <div class="traits">
            <span class="trait">Trait</span>
            <span class="trait">Trait</span>
            <span class="trait">Trait</span>
          </div>
          <p style="margin-top: 16px; font-size: 13px; color: var(--gray-600)">
            这里写一段补充说明。
          </p>
        </div>
      </div>
    </section>

    <main class="conversation">
      <div class="conv-header">
        <h2>The Dialogue</h2>
      </div>

      <section class="dialog">
        <p class="speaker q">Interviewer</p>
        <p class="content">问题句子……</p>
      </section>

      <section class="dialog">
        <p class="speaker a">Guest</p>
        <p class="content answer">
          回答段落……<span class="highlight">需要强调的句子。</span>
        </p>
        <p class="content answer">第二段回答……</p>
        <blockquote>“可作为引文/摘录的句子。”</blockquote>
      </section>
    </main>

    <div class="divider">
      <hr class="divider-line" />
    </div>

    <section class="closing">
      <p class="closing-quote">
        “用一句收束全篇的核心引言。<br />可换行以形成节奏。”
      </p>
      <p class="closing-attr">— Name</p>
    </section>

    <footer>
      <div class="footer-inner">
        <p class="footer-note">
          备注：来源、版权说明、用途说明。避免整段原文复制，优先摘要与链接。
        </p>
        <p class="footer-meta">Series · YYYY</p>
      </div>
    </footer>
  </body>
</html>

